# Отчёты по ачивкам

Сюда `bot/tools/sigame_achievements.py` сохраняет TXT-отчёты после разбора
журналов SIGame:

```text
game-log-<дата>-achievements.txt
```

Сами отчёты являются локальными результатами игры и не добавляются в Git.
