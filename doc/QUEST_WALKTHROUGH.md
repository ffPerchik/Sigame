# ARG «Аргус-1001» — разгадка (ведущий)

Шесть независимых узлов из хаба. В каждом — несколько слоёв.
Игрокам этот файл не отдавать.

Фрагменты: **ОТКРОЙ · УСЛЫШЬ · ПРОЧИТАЙ · НАБЛЮДАЙ · ВЫЧИСЛИ · ЗАВЕРШИ**

Пересборка медиа: `python3 tools/make_quest_assets.py`

---

## 0. Вход

`zengame.siq` → zip → `START.txt` (два Base64) → `/start ARGUS1001` →
`start_gate` (апрув ведущего) → `prologue` → `hub`.

Подсказки: `/addhint @nick n`. На старте у игрока 0.

Выездов на локации **нет**. QR на улице больше не нужны.

---

## N1 · СЛОИ СНИМКА

| слой | приём | ответ |
| --- | --- | --- |
| кадр `n1_card.jpg` (не stego финала SIGame) | перевёрнутый текст `ЬТРУНВ ИРТОМС` | **СМОТРИ ВНУТРЬ** |
| EXIF | Artist `G-CHANNEL`, комментарий про зелёный | **зелёный** / green |
| `n1_layers.png` (document) | почти невидимое `LSB-B` в канале G | указатель |
| LSB канала B | UTF-8 до нуля: `18-12-15-09` = A1Z26 | **СЛОИ** |

Фрагмент: **ОТКРОЙ**.

Питон: `from PIL import Image; import numpy as np` — младший бит синего канала.

---

## N2 · ЭХО

| слой | файл | ответ |
| --- | --- | --- |
| реверс + Морзе | `n2_reversed.wav` | **КЛЮЧ** |
| спектрограмма | `n2_spec.wav` | **ФИБО** |
| Фибоначчи по словам | `n2_verse.png` слова 1–3 | **ЭХО** (ЭТОТ ХРУПКИЙ ОТКЛИК) |

Русский Морзе: К `-.-` Л `.-..` Ю `..--` Ч `---.`

Фрагмент: **УСЛЫШЬ**.

---

## N3 · ТЕТРАДЬ

| лист | приём | ответ |
| --- | --- | --- |
| I `n3_pigpen.png` | масонский / pigpen | **РЕШЕТКА** |
| II `n3_rail.png` | rail fence, 3 рельса, шифр `ВЕИНРЖ` | **ВИЖНЕР** |
| III `n3_vig.png` | Виженер, ключ РЕШЕТКА, шифр `БЧИУЖК` | **СТРОФА** |
| IV `n3_book.png` | книга 1.2 2.3 3.3 4.2 5.3 6.2 | **ИСТИНА** |

Алфавит везде 32 буквы без Ё.

Фрагмент: **ПРОЧИТАЙ**.

---

## N4 · ОДИН КАДР

| слой | где | ответ |
| --- | --- | --- |
| вывески | почти все кадры `n4_walk.mp4` / `n4_signs.png` | **ЛИНЗА** |
| один кадр | вспышка, hex UTF-8 `d0a2d095d09dd0ac` | **ТЕНЬ** |
| обломки | `n4_shards.png` 2×2 | **КАДР** |

Фрагмент: **НАБЛЮДАЙ**.

---

## N5 · СЧЁТ

| слой | где | ответ |
| --- | --- | --- |
| 6-бит A1Z26 | блок I `n5_table.png` | **МОДУЛЬ** |
| магический квадрат 3×3 | блок II, порядок 1…9, отрезать XXXX | **ЧИСЛО** |
| HTML-комментарий | `n5_ledger.html` → `d0bb d0be d0ba` | **ЛОК** |
| замок | `n5_lock.png` ряд 1 1 2 3 5 8… без единиц | **2358** |

Фрагмент: **ВЫЧИСЛИ**.

---

## N6 · ЧЕТЫРЕ ЩЕКОЛДЫ

Доска `n6_locks.png`. Цепочка:

1. A1Z26 `32-14-31-32-08` → **ЯНЮЯЗ**
2. Атбаш → **АТБАШ**
3. Rail-3 `ВЕИНРЖ` → **ВИЖНЕР**
4. Виженер, ключ АТБАШ, шифр `ПАСТШЛ` → **ПОРТАЛ**

Фрагмент: **ЗАВЕРШИ**.

---

## Чеклист ведущего

- [ ] `.env`: `BOT_TOKEN`, `HOST_ID`, `BOT_USERNAME`, `ENTRY_CODE=ARGUS1001`
- [ ] `python3 tools/make_quest_assets.py` (если ассеты трогали)
- [ ] `cd bot && python3 bot.py`
- [ ] Игрокам — пакет `zengame.siq` (вход через стегано финала SIGame)

Команды: `/stats` `/addhint` `/setstage` `/pending` `/approve` `/reset` `/broadcast`
